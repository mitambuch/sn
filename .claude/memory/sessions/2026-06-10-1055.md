---
id: session-2026-06-10-1055
date: 2026-06-10
type: session
tags: [#content, #i18n, #testing, #client-specific]
scope: client-specific
status: active
---

# Session — Team cards: per-member data (no Valmont leak)

Owner forwarded a client brief (`public/brief_claude_code_saw_next.md`) and
said "lance ça et commit entre chaque étape". The brief: the team section
(Interlocutor S09) leaked Valmont's contact/function/CTA onto every other
profile — fix the **data logic**, no graphic redesign.

## Flagged before coding

The current behaviour was a **documented owner direction** (focal-only
channels, "Contact via Valmont" pill, CTA → Valmont). The brief reverses
it. Surfaced the reversal; owner had already approved by forwarding the
brief. Decision recorded:
[[team-cards-per-member-contacts]].

## Done — branch `fix/team-member-cards` (off main, NOT prelaunch-hardening)

3 commits, each validated:

1. **feat e000d16** — `src/features/landing/teamData.ts`: typed
   `TEAM_MEMBERS` (5) as single source of truth — sectorTitle +
   functionLabel (tri-locale fr/en/es), phone/email, optional
   whatsapp/linkedin, slug/isFocal/order. Helpers `toTelHref`,
   `toWhatsAppHref` (wa.me derived from phone).
2. **fix d2643fa** — `Interlocutor.tsx` reads the active member's record:
   every profile shows its OWN channels (not focal-only), per-member
   function line (was static "Fondateur opérationnel"), dynamic
   "Voir <name>" CTA anchored to the member slug. Removed FOCAL_CHANNELS /
   HARDCODED_MEMBERS / resolveTag. i18n: added `seeMember`, dropped dead
   `role`/`viaFocal`/`seeFocal` + per-member `tag` keys (fr/en/es). Bios
   unchanged (Sanity→i18n path preserved).
3. **test 4b1b4fa** — `teamData.test.ts` (15 tests): wa.me table, distinct
   phones (contamination guard), one focal, tri-locale fields, no "#"
   LinkedIn, brief values. Coverage ratchet green (teamData 100%).

Validation: build green · 537/537 unit · lint/typecheck/studio/
sanity-schema/i18n green · coverage exit 0. `memory:index:check`
date-drift is a pre-existing flake → [[memory-index-check-date-drift]].

## ACTION HUMAINE / follow-up

- **LinkedIn URL**: none exists in the project (was `'#'`). Row hidden
  until a real SAW Next LinkedIn url is added to `teamData.ts`. Not
  fabricated (brief forbids it).
- **CTA target**: anchors to `#<slug>` — no `/team/<slug>` pages exist.
  Repoint when built.
- **public/ leak**: `public/brief_claude_code_saw_next.md` sits in the
  deploy-served `public/` dir (untracked, so safe unless committed).
  Recommend moving it out of `public/`.
- Branch unmerged (matches owner's habit of holding branches pre-launch).

## Next

Owner: provide LinkedIn URL (optional), move the brief out of public/,
review the branch, then merge alongside prelaunch-hardening.
