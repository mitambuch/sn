---
id: 2026-05-13-1701-salva-autonomy-demo-ready
date: 2026-05-13
type: session
tags: [#design, #sanity, #auth, #release, #client-specific, #p0]
scope: client-specific
status: active
---

# Session — Salva autonomy démo-ready (13:38 → 17:01)

3h23 marathon après-midi. Démarrage à 13:38 sur l'audit "qu'est-ce qui
reste à câbler" → bascule progressive en mode "préparation démo client"
avec l'owner qui itère sur le rendu visuel + correctifs UX.

## Releases / merges

| Tag (informel) | Commit | Sujet |
|---|---|---|
| v0.8 → v0.8.1 | `cb10da9` | fix: hide chrome until loader done |
| v0.8.1 → v0.8.2 | `910fa95` | chore: sanity deploy hostname + appId |
| v0.8.2 → v0.9 | `776439c` | feat: Salva autonomy Studio share flow |
| v0.9 → v0.9.1 | `4d84e84` | feat: Sanity seed + /exemple landing |
| v0.9.1 → v0.9.2 | `8a0770e` | feat: 6-char codes + OTP /exemple |
| v0.9.2 → v0.9.3 | `7055602` | fix: example page layout |
| v0.9.3 → v0.9.4 | `46f5811` | fix: animate-important on example card |
| v0.9.4 → v0.9.5 | `f9edb43` | feat: action-first Dashboard + RPC stats |
| v0.9.5 → v0.9.6 | `84cf879` | fix: filter system docs from activity |
| v0.9.6 → v0.9.7 | `586de7a` | fix: APERCU short-circuit + brand Dashboard |
| v0.9.7 → v0.9.8 | `22c78bf` | fix: mock fallback so /share/APERCU renders |
| v0.9.8 → v0.9.9 | `b326503` | fix: diacritic tolerance + Entrer button |
| v0.9.9 → v0.9.10 | `86a1d4e` | feat: fiche box + Interlocutor promote + mobile CTA |
| v0.9.10 → v0.9.11 | `791892a` | feat: Interlocutor autoplay + progress bar |

**32 commits sur main depuis v0.8 (d5e0361).**

## Ce qui a livré (par chantier)

### Bloc 1 — Loader UX (matin tardif)

1. **Loader cinétique cache chrome top** : `onDone` callback → Home
   gate la TopCornerChrome avec fade-in 600ms.

### Bloc 2 — Infra cabling (early afternoon)

2. **Sanity Studio déployé** sur https://sawnext-studio.sanity.studio/
   - Project ID `k2xrvftw` créé, studio/.env câblé, deploy via
     `pnpm run deploy` après pin `studioHost` + `appId` dans
     sanity.cli.ts (skip prompts interactifs Windows).
3. **Vercel env vars** : VITE_SUPABASE_URL/ANON_KEY,
   VITE_SANITY_PROJECT_ID/DATASET/API_VERSION configurés + redeploy.
4. **Supabase migration 0005** (share_codes) appliquée par l'owner.

### Bloc 3 — Salva autonomy share flow

5. **Document Action "Partager cette fiche…"** sur les 7 types
   shareable, dialog qui s'adapte à `visibility` (private/public/
   shareCode). Bouton "Ouvrir le générateur" deeplink admin frontend
   avec docType+docId pré-remplis (plus de copier-coller _id).
6. **AdminShareCodes lit ?docType=X&docId=Y** depuis URL.
7. **ShareActionRow atom** + lib/sharing.ts (WhatsApp/Email/SMS/Copy
   helpers + brand-voice FR/EN message templates).
8. **SharePage rendu réel** via GROQ_SHARED_FICHE + useSanityItem.

### Bloc 4 — Démo: seed + /exemple + 6 chars

9. **Sawnext seed fixture** (14 docs : 3 pages + 8 fiches + 3 team
   members) + script `pnpm sanity:seed:sawnext`.
10. **Page /exemple** publique avec OTP boxes 6 chars + brand
    SAW↗NEXT + box formulaire + bouton "Retour sur le site".
11. **6-char codes sans préfixe SAW** (migration 0006 +
    normalizeShareCode tolère ancien format SAW- pour rétro-compat).
12. **APERCU = code démo permanent** dans share_codes + DEV stub
    inconditionnel dans lib/shareCode.ts (marche sans Supabase).

### Bloc 5 — Studio caractère + Dashboard action-first

13. **Studio Dashboard refonte action-first** : drop "Bienvenue 👋",
    4 tuiles principales (Créer une offre / une clé / Voir les
    demandes / Gérer les clients).
14. **Live badges** via RPC `studio_dashboard_stats()` (migration
    0007) : pending inquiries (pulse rouge si > 0), active share
    codes, total clients. Fetch via PostgREST + Supabase
    publishable key (Studio bundle).
15. **Filter system docs** out of Sanity activity feed
    (`_.schemas.*`, `_.groups.*`).
16. **Dashboard caractère SAW NEXT** : monochrome strict (drop
    bleu/violet/vert/orange/rouge — viole brand rule), Geist Mono
    partout, hairline borders, generous whitespace, header
    "SAW↗NEXT — ATELIER", indicators pill-shaped uppercase.

### Bloc 6 — Démo robustesse + UI fixes

17. **SharePage fallback mocks** : quand Sanity vide, render le
    contenu réaliste depuis src/mocks (Gala ONU avec hero + summary
    + description + programme).
18. **Diacritic tolerance** `normalizeShareCode('aperçu')` →
    'APERCU' (NFD strip).
19. **Manual Entrer button** + visible demo hint "Tester avec APERCU"
    sur /exemple.
20. **SharePage box catégorisée** : tout dans une Card unique,
    MetaList type-aware (Évènement → date+lieu+capacité+dress code+
    programme Timeline ; Propriété → kind+mode+surface+chambres+SDB+
    terrain+région ; Garde-temps → marque+modèle+ref+année+boîtier+
    matériau+état+full set ; etc. pour les 7 types).
21. **Interlocutor promotable** : clic sur Harry ou Bokar → focal,
    Salva reste contact opérationnel canonique.
22. **TerminalBar mobile** : 3 CTA tiennent sur 1 ligne à 320px
    (labels tronqués "Demander" / "Espace" + padding réduit).
23. **Interlocutor autoplay 8s** + progress bar 3 segments en bas
    + hover lock + `md:min-h-170` box fixe + Contact bloc au bas
    via `mt-auto`.

## Décisions notables

- **Code démo = APERCU** (6 chars, alphabet conforme,
  mémorisable, brand-aligned). Owner explicit "pas de SAW-DEMO".
- **DEMO_RESULT inconditionnel** dans consumeShareCode — la démo
  marche même sans Supabase / sans migration / sans seed Sanity.
  Décompoupler l'infra des démos client.
- **Studio Dashboard couleurs candy → monochrome strict** (sauf
  un coral désaturé `#d65a3a` pour le pulse pending inquiries —
  signal d'urgence légitime). Brand rule "monochrome only" tenue.
- **Sanity Studio Tool iframe SKIPPÉ** — Document Action +
  deeplink admin frontend plus simple. Auth admin déjà sur Vercel.
- **Mirroir Sanity (Hero/Manifesto/etc.)** DÉFERRÉ — gros chantier
  ~1h+, owner peut le faire après la démo.
- **OtpInput atom réutilisable** dans src/components/ui (auto-
  advance + paste + diacritic strip + danger variant).

## Frictions notables

- **Sanity CLI `sanity deploy` interactive prompt** ne marche pas
  avec stdin pipe sous Windows PowerShell. Fix : pinner `studioHost`
  + `deployment.appId` dans `sanity.cli.ts`.
- **Cédille `ç` rejetée par OTP regex** — owner tapait "aperçu"
  (vraie orthographe FR), boxes bloquaient à 5/6. Fix : NFD strip
  diacritics dans `normalizeShareCode` ET dans le keypress handler
  de OtpInput.
- **System docs `_.schemas.*` polluent activity feed** Studio
  Dashboard → ajouter filter GROQ `!(_type match "system.*") &&
  !(_id match "_*")`.

## RELEASE CHECK

```
Commits sur main depuis v0.8 (d5e0361) : 32
  - feat(loader+landing) chrome hide : 2
  - chore(studio) deploy hostname+appId : 1
  - feat(share/studio) Salva autonomy doc action : 4
  - feat(demo) seed + /exemple + 6chars : 6
  - fix(demo) APERCU short-circuit + mock fallback : 3
  - feat(studio) Dashboard action-first + RPC + brand : 5
  - fix(example) layout + diacritic + Entrer + animate-important : 5
  - feat(share) fiche box catégorisée : 1
  - feat(interlocutor) promote + autoplay + progress : 2
  - feat(landing) TerminalBar mobile compression : 1
  - chore(release) merges : 2
Validate pipeline : ✅ validate:fast vert sur toutes les passes.
Recommandation : WAIT — owner attend la fin de Codex (session parallèle)
  avant de cut un tag GitHub. Le merge final intègrera leurs commits +
  les miens.
Raison : ne pas tagger v0.9.0 avant que tout soit consolidé. Quand
  Codex push, regrouper + release-it minor "Salva autonomy + démo
  client v0.9.0".
Next milestone : v0.9.0 "Salva autonomy v1 + démo client-ready" target
  2026-05-13 soir ou 2026-05-14 matin une fois Codex mergé.
```

## Ce qui reste ouvert

- **Mirroir Sanity** : wiring Hero/Manifesto/Presentation/Domains →
  page-home Sanity (fallback i18n). Owner peut décider de le faire
  ou pas selon réception client.
- **Tech debt pré-existant non traité** :
  - Raw `string` fields dans schemas event/property/timepiece (~14
    fields à convertir en localeString OU ajouter à ALLOWED_RAW).
  - 2 FR strings hardcodés dans AdminShareCodes (placeholder note +
    empty label) à i18n-iser.
- **Clients & catégories Sanity↔Supabase** : architecture mûre
  pour sa propre session dédiée (3 questions à poser owner : grain
  des catégories, permissions, sync direction).
- **Document Action Studio "Générer code direct"** (sans deeplink
  admin) : nécessite Supabase auth dans Studio, complexité élevée,
  skipper sauf si owner remonte la friction.
- **Sanity seed à pousser** : owner doit générer SANITY_WRITE_TOKEN
  + run `pnpm sanity:seed:sawnext`. Pas critique pour la démo
  client grâce au fallback mocks dans SharePage.
- **Migration 0006 + 0007 à appliquer côté Supabase** par l'owner
  via SQL Editor (sinon : APERCU marche via short-circuit, badges
  Dashboard restent en "—").

## Cross-refs

- Session précédente : `sessions/2026-05-13-1410.md` (Salva
  autonomy première vague — Document Action + AdminShareCodes
  upgrades).
- Décision design monochrome : `decisions/2026-05-12-brand-
  identity-saw-next.md`.
- Inspiration Sanity Dashboard : étoiles-aux-atomes pattern
  (mentionnée par owner, repo privé non lu directement).
- Frictions :
  - Sanity CLI Windows interactive prompt → fix dans `sanity.cli.ts`.
  - OTP cédille → fix dans normalize + key handler.
