---
id: session-2026-05-27-1430
date: 2026-05-27
type: session
tags: [#release, #client-specific, #sanity, #content, #auth, #security, #p0]
scope: client-specific
status: active
---

# Session — Team personas rework + backend consolidation push (demo-readiness)

## Goal

Owner request, two-phase :

1. **First**, ajouter 5 nouvelles personas à la section équipe S09 du
   landing — anagrammes des vraies identités, garder discrétion :
   *"Le but est de garder quelque chose de crédible, élégant et cohérent
   avec l'univers SAW Next, sans déformer la réalité ni trop exposer
   les vraies identités."*
2. **Then**, *"avancer sur le backend de consolider le tout… que TOUT
   devienne fonctionnel et que je puisse montrer à mon client tu
   comprends ?"* → ENORME audit + plan d'action + exécution autonome
   + commit à chaque grosse étape.

Session lasted ~2h (12:37 → 14:30 Europe/Zurich).

## What landed (5 commits across 2 branches)

### Branch 1 — `feat/team-personas-rework` (merged → main)

**Commit `a87ef4b` — feat(landing): expand interlocutor to 5-persona network**
- 25 files / +308 / −253. Identity scrub site-wide.
- Interlocutor.tsx : MEMBERS 3→5 (Valmont focal + Harvy/Lucian/Tavio/
  Sergio network), MemberKey type renamed, FOCAL_KEY constant
- Progress bar consolidated : 0X numbering dropped, names centered,
  single border-r divider replacing gap-px, grid-cols dynamic
- locales/{fr,en}.json : every "Salvatore" → "Valmont", every
  "Montemagno" → "Seragone Mato". Key rename
  `salvaReassurance` → `focalReassurance` + 3 component refs
- mocks/users.ts : `usr-op-salva` → `usr-op-valmont`, full name +
  email propagation across 8 mock entries
- AuthContext + useInvitationsAdmin + RequireAuth test : identity
  propagation
- 5 page-level scrubs (AccountDashboard, Onboarding, ExamplePage,
  SharePage, AdminDashboard)
- studio/fixtures/sawnext-seed.json : team block rebuilt 3→5
  entries with order 1-5, founders narrative reworked to fit
  single-founder+network reality
- studio/schemas/documents/teamMember.ts : schema description +
  inline comments updated

Final grep `Salvatore|Montemagno|Tamburini|Harry Novillo|Bokar
Guissé|usr-op-salva|salvaReassurance|salvaBio|team-salvatore` →
**0 hits**.

**Merge `6154b8f`** → main via `git merge --no-ff` after owner
explicit consent.

### Branch 2 — `feat/demo-readiness` (current, NOT merged)

**Commit `8c2e478` — feat(landing): wire Interlocutor to Sanity teamMember docs**
- New `hooks/useTeamMembers.ts` : GROQ fetch `*[_type=="teamMember"]
  | order(order asc)` returning `{members, loading, error,
  usingFallback}`. Falls back when Sanity unconfigured or dataset
  empty
- Interlocutor.tsx refactor :
  - HARDCODED_MEMBERS preserved as deterministic fallback
  - members + focalDefaultKey + sequence derived (useMemo) from
    Sanity when available, hardcoded otherwise
  - MemberKey widened to string (Sanity keys dynamic)
  - `resolveTag` / `resolveBio` helpers : Sanity[locale] → Sanity.fr
    → i18n fallback (3-tier per i18n-sanity rule #4)
  - `effectiveFocalKey` derived (not synced via useEffect) to satisfy
    react-hooks/set-state-in-effect lint
  - Progress bar `gridTemplateColumns: repeat(N)` dynamic
- FOCAL_CHANNELS (phone/email/wa/li) kept hardcoded for security —
  operational contact never goes blank, never editable via CMS
- studio/scripts/seed-sawnext.js : header comment refreshed (3 → 5,
  names corrected)

**Commit `ba4d632` — docs(ship): refresh audit + demo checklist**
- docs/SHIP-AUDIT.md : full rewrite reflecting 2026-05-27 state
  (v0.2.0 shipped, migrations 0008-0010 written-not-applied, Sanity
  team wiring landed, Phase C in flight, 6-axis verdict snapshot)
- docs/DEMO-CHECKLIST.md : NEW exhaustive paste-ready owner guide
  - 5 steps : Supabase project + 11 migrations + Vault, Sanity
    project + seed + promote, first admin + first code, Vercel
    deploy + env vars, full smoke test (9 verification steps)
  - Common gotchas section (function-not-found / permission-denied /
    magic-link-redirect / silent-Resend / build-vs-dev drift)
  - Post-demo follow-ups

**Commit `a69c6ef` — feat(account): saved items via Supabase optimistic-local + background sync**
- supabase/migrations/0011_saved_items.sql : NEW table with composite
  PK (user_id, module, slug), RLS self-only "for all" policy, grants
  restricted to select/insert/delete (no update path), index on
  user_id, 5 verification queries at the bottom
- hooks/useSavedItems.ts : optimistic-local-first + background-sync
  - localStorage layer kept as primary store (no loading state on
    heart toggle — 10+ consumers don't get async cost)
  - useEffect subscribes to `supabase.auth.onAuthStateChange` to
    track userId (NOT via AuthContext — keeps the hook
    self-contained, no AuthProvider required at consumer site,
    no test ripple)
  - pullFromSupabase on userId change : union-merge into local cache
  - pushToSupabase on toggle : upsert on add, delete on remove —
    fire-and-forget, eventually consistent
- HeartButton + AccountSaved page + SimilarItemsStrip : zero
  call-site changes (public API unchanged)
- Conflict model documented in commit body : last-write-wins on
  Supabase, local caches reconcile to whichever local state they
  hold. Acceptable for wishlist UX.

## What's blocked on owner action

The 4 commits on `feat/demo-readiness` cover ALL the code work for
demo readiness. Remaining items require owner credentials and are
documented in `docs/DEMO-CHECKLIST.md` :

1. **Apply migrations 0008 + 0009 + 0010 + 0011** to Supabase project
   (SQL paste in Dashboard or `supabase db push`)
2. **Create Sanity project** + grab projectId / dataset / write token
   → `.env.local`
3. **Run `pnpm sanity:seed:sawnext`** to push 5-team + landing
   singletons + catalogue fixtures
4. **Set `RESEND_API_KEY` in Supabase Vault** (and optionally
   `OPERATOR_EMAIL`)
5. **Deploy to Vercel** with env vars set (mirror of `.env.local`)
6. **Promote first admin** via SQL UPDATE on `profiles.role`
7. **Generate first invitation code** via `/admin/invitations`
8. **Smoke test** the 9-step flow from incognito

## Friction encountered

- **`react-hooks/set-state-in-effect` lint** rejected the initial
  Interlocutor refactor where I had `useEffect` syncing focalKey to
  focalDefaultKey on member list change. Fixed by deriving
  `effectiveFocalKey` during render instead. Cleaner anyway.
- **`useAuth must be used inside <AuthProvider>`** broke 3
  AccountCatalogue tests after wiring useSavedItems to AuthContext.
  Fixed by reading userId directly from `supabase.auth.getSession()`
  + `onAuthStateChange` listener — decouples the hook from the
  AuthContext entirely. Tests pass with zero modification.
- **`pnpm validate` build step fails** without `VITE_APP_URL` set
  locally. Documented in DEMO-CHECKLIST.md — vite.config.ts has a
  prod-build guard that throws if neither VITE_APP_URL nor
  VERCEL_URL is set. Local validate works with
  `VITE_APP_URL=http://localhost:5173 pnpm build` ; deploy works
  because Vercel injects VERCEL_URL.

## Patterns reused

- **Sanity hook pattern** (useTeamMembers) mirrors useAdminCatalogue +
  useLandingContent : `{ data, loading, error, usingFallback }`
  shape, `cancelled` flag for unmount safety, fallback semantics
  when `hasSanity === false` or dataset empty
- **Optimistic-local + background-sync** pattern (useSavedItems) is
  the recipe documented in `SHIP-AUDIT.md` (2026-05-14 draft) — now
  shipped
- **Auth-decoupled hook** : reading `supabase.auth` directly instead
  of `useAuth` keeps test surface clean. Future hooks doing
  user-scoped DB writes should follow this pattern

## Files touched (high level)

- **Created** : `src/hooks/useTeamMembers.ts`,
  `supabase/migrations/0011_saved_items.sql`,
  `docs/DEMO-CHECKLIST.md`, this journal
- **Modified** : `src/features/landing/Interlocutor.tsx` (large
  refactor for Sanity wiring), `src/hooks/useSavedItems.ts` (Supabase
  sync), `docs/SHIP-AUDIT.md` (full rewrite),
  `studio/scripts/seed-sawnext.js` (comment refresh),
  + 25 files from the team-personas branch already merged

## What to test

Owner should follow `docs/DEMO-CHECKLIST.md` end-to-end. Critical
verification :

1. After applying migrations : run the verification queries at the
   bottom of `0010_security_hardening.sql` AND `0011_saved_items.sql`
2. After Sanity seed : open Studio → confirm 5 team members visible
   in order 1-5, Valmont marked focal (⭐ in preview)
3. On the deployed site : sign in on two devices, heart an item on
   device A, verify it appears on device B after refresh
4. From incognito : "Demander un accès" → check operator inbox
   receives the Resend-delivered email

## RELEASE CHECK

```
Commits depuis dernière release (v0.2.0) : 5
Types : feat(3) + docs(1) + chore(merge)(1)
Recommandation : release v0.3.0 — minor bump
Raison : 3 feat commits (5-persona rework + Sanity team wiring +
  cross-device wishlist), one of which is user-visible cross-device
  persistence. Plus the owner doc deliverable. Coherent demo-readiness
  release. Recommend release AFTER owner has confirmed visual on
  /#s09 with Sanity wired + applied 0011 migration.
Next milestone : v0.4.0 = post-demo hardening (Sentry session
  replay, OPERATOR_EMAIL wired, content gate in CI, AccountSaved
  cross-device tests)
```

## Cross-refs

- `docs/SHIP-AUDIT.md` — refreshed 2026-05-27 state
- `docs/DEMO-CHECKLIST.md` — owner action list
- `decisions/2026-05-14-atomic-redemption-rpc.md` — 0008 design
  precedent
- `decisions/2026-05-14-sanity-landing-singleton.md` — Sanity
  fallback pattern reused for team wiring
- `frictions/2026-05-11-supabase-vault-secret-naming.md` — Vault
  naming for RESEND_API_KEY (relevant to demo checklist step 1.5)
- `sessions/2026-05-27-1230.md` — preceding session (Phase 1-5
  security audit + v0.2.0 release)

## Continuation 14:59 → 15:23 — Phases D + E + F + G + H

Owner returned 14:59 with directive : *"0 régression visuelle tu peux
continuer ! je vais faire les étapes pour cabler après, toi, tu dois
continuer à travailler sur l'architecture et vraiment consolider tout
ce qu'il faut"*. Autonomous push for 25 min ; 5 additional commits.

### Phase D — `ab10e29` feat(inquiry): unify all drawer submissions to Supabase inquiries

**Critical gap found** : 5 of the 6 request drawers were pure UI
simulators (setTimeout + toast, NO DB write). Only the canonical
InquiryDrawer ("Express interest" on catalogue cards) persisted.
Every other surface (FreeForm inquiry, JetCharter, Bespoke,
ScheduleCall, ConciergeRequestWizard) showed success toasts but
nothing reached Supabase, no Resend email fired, no operator trace.

Fix : `src/lib/inquiry.ts` unified helper with single `canPersist`
heuristic + `submitInquiry({source, message, targetId?, userId})`
returning `{ok, error?, simulated}`. Every drawer refactored to
flatten its structured fields into the message body + call the
helper. ConciergeRequestWizard's CATEGORY_TO_SOURCE map routes
real-estate→property, timepiece→timepiece, art→artwork, experience
→event, travel→journey, other→concierge.

### Phase E — `fb0f422` test(inquiry,team)

19 new tests : 13 for the inquiry helper (simulator/Supabase/edge
cases), 6 for useTeamMembers (fallback / loading / Sanity-mapping
/ error). Module-level dynamic-getter mock pattern (`vi.mock` with
getters reading test-mutable refs) lets each test set up its own
hasSupabase/sanityClient state. 457 → 476 tests.

### Phase F — `de0c750` feat(account): live-session profile + edit drawer

**Gap** : AccountProfile read `currentUser` mock unconditionally —
even with a real session, page showed "Hugo Méredith". Migration
0009 added profiles.phone, Onboarding writes it on step 2, but no
edit surface existed after.

Fix : `AccountProfile.tsx` reads `useAuth().session?.user` (fallback
to mock when no session). New `features/account/ProfileEditDrawer.tsx`
right-side drawer matching the inquiry-drawer chrome. Editable
columns match exactly the migration 0010 column whitelist
(full_name + phone + contact_preference + locale). Single UPDATE
filtered to current user_id. Simulator path preserves demo UX.

### Phase G — `bf4ea5e` feat(admin): inquiry status transitions

**Gap** : AdminInquiries was read-only — operator could see new
requests but couldn't progress them through workflow. RLS already
supported admin UPDATE.

Fix : `useInquiriesAdmin` extended with `updateStatus(id, next)`
optimistic mutation (local overrides map for instant card jumps +
Supabase write + rollback on remote failure). AdminInquiries
kanban gets a per-card `<select>` with all 5 statuses. Properly
labelled with htmlFor + aria-label for a11y.

### Phase H — `a3a3869` feat(access): persist landing access requests

**Gap** : AccessRequestModal's 3-step wizard captured 7 fields but
nothing happened with the data — pure simulator with a "stub routed
to existing Resend pipeline once Netlify function accepts" TODO.
The "Demander un accès" CTA is the #1 entry point for new client
leads — demo-broken without persistence.

Fix : `supabase/migrations/0012_access_requests.sql` adds a
dedicated table (access_request_status enum + 7 form columns +
status) with RLS = anon-insert-only (write-only — no read-back to
prevent id-guessing enumeration) + admin select/update for triage.
Trigger `notify_new_access_request` reuses the escape_html() +
Vault-backed pattern from migration 0010 to email the operator.
AccessRequestModal swaps the setTimeout for a real
`supabase.from('access_requests').insert()` with simulator fallback.

### Cumulative state

10 commits on `feat/demo-readiness` branch (8 feat + 1 docs + 1 chore-
memory, plus 1 merge in main). 476 tests passing. lint + typecheck +
studio:typecheck green. Build green with VITE_APP_URL set.

Migration list updated : 0008 + 0009 + 0010 + 0011 + 0012 all need
owner-side apply. DEMO-CHECKLIST.md updated with the new 12-step
smoke test covering every demo path.

## Next session (refresh)

1. **Wait for owner to apply 5 migrations** (0008-0012) + create
   Sanity project + set Vault + deploy → smoke-test the 12-step flow
2. **Release v0.3.0** once confirmed live — feat(landing) +
   feat(account) × 2 + feat(inquiry) + feat(admin) + feat(access)
   = clear demo-readiness milestone
3. **Post-demo** : triage anything that broke / surprised the client
   into `feedback/2026-05-XX-demo-debrief.md`. Likely next sprint
   topics : Sentry session replay, content gate in CI, /admin/
   access-requests page (Phase H deferred its UI), AccountPreferences
   persistence (notification toggles UI-only today)
