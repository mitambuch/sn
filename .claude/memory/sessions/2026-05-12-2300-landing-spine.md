---
id: 2026-05-12-2300-landing-spine
date: 2026-05-12
type: session
tags: [#design, #workflow, #client-specific, #active, #p0]
scope: client-specific
status: active
---

# Session — Landing publique spine (22:44 → 23:13)

Suite directe du journal
[`2026-05-12-2200-preloader-baseline-addendum.md`](2026-05-12-2200-preloader-baseline-addendum.md).
Owner pivot 22:38 vers nouvelle conversation pour porter le mockup HTML
(produit avec autre instance Claude.ai sur Desktop) dans le repo. Cible :
landing publique présentable demain matin pour livraison client.

## Contexte d'entrée

- Mockup HTML 2139 lignes / 68 KB, écrit par owner avec une autre instance
  Claude qui n'a pas le contexte projet. Owner a explicitement cadré :
  *"si il dit de la merde c'est ici que ça fait foi, lui il a pas tout le
  contexte donc on va être prudent ok ?"*
- Mockup déposé d'abord `public/mockup.html` (mauvais — public/ va en
  prod). Déplacé vers `docs/landing-mockup-v5.html` au cours de la
  session.
- 9 sections validées + ordre + dispositions + terminal-bar bottom +
  INDEX overlay + 4 marquees. **Copy** validé comme base de travail.

## Décisions explicites prises avec owner

| # | Choix | Verdict | Raison |
|---|---|---|---|
| (a) Palette | Tokens projet (`#bcbcbc`/`#2a2a2a`) **vs** mockup (`#fafaf8`/`#0a0a0a`) | **Projet** | "On garde tout le style de notre site, on doit appliquer la structure" |
| (b) Couleurs d'appoint | Vert `#00ff95` ticker autorisé ? | **OK** parcimonieux | "on peut utiliser des couleurs d'appoint biensûr" — mappé sur `--color-success` |
| (c) Cycling word | Option A in-line vs Option B sous-ligne | **A — voix [basse / tenue / feutrée / retenue]**, 4s/mot | "On suit ta reco" |
| (d) Scope tonight | Spine seul (S01/S03/S08/S09/footer) vs all-in 9 sections | **Spine** | 2h window confirmé par owner — 9 sections impossibles dans le temps |

Owner stance générale : *"travaille maintenant aufaite il est que 23h
j'ai 2heures devant moi"* → batch mode strict, pas de stop pour
clarification.

## Conflits détectés mockup vs règles projet (gérés par moi)

🔴 Bloquants brand/règles (l'autre instance n'a pas vu) :

1. **Vert `#00ff95`** sur dot live + hover exp-card → remplacé par
   `bg-success` token (cohérent avec les status colors existants).
2. **Copy FR hardcodé** → toute la copy passe par i18n keys (`landing.*`
   dans `fr.json` + `en.json` parité, trad EN sommaire à raffiner
   demain).
3. **Salva info en clair dans mockup** → static dans `Interlocutor.tsx`
   pour tonight. Sanity `interlocutor` singleton à créer demain.

🟡 Tech corrigés :

4. **Geist Mono via Google Fonts CDN** → utilise `font-mono` token
   (Geist Mono Variable local déjà bundlé pour BrandPreloader).
5. **`mix-blend-mode: difference` top-bar** → remplacé par span fg/bg
   simple (Safari iOS risk évité).
6. **`overflow-x: hidden body`** → pas appliqué (conflit Lenis).
7. **9px font terminal-bar mobile** → remonté à 10-11px + icône-only
   sous sm (touch target 44px gardé via padding).
8. **Ticker `display: none` mobile sans fallback** → ticker masqué
   desktop only ; mobile garde les 2 CTAs Espace privé / Demander un
   accès (always-on, conversion path).
9. **Palette mockup inversée** → entièrement repalettée sur tokens
   projet (#bcbcbc bg / #2a2a2a fg).

## Ce qui a été shippé (commit à suivre)

### Nouveaux fichiers (`src/features/landing/`)

- `useCyclingWord.ts` — hook typographic cycling 4s/mot avec respect
  prefers-reduced-motion
- `SectionTag.tsx` — atom eyebrow "↘ NUM / Label"
- `TopProgress.tsx` — barre scroll progress fixed 2px en haut
- `Marquee.tsx` — bande défilante infinie, variant dark/light,
  pause-on-hover via group-hover
- `IndexOverlay.tsx` — overlay full-screen slide-down avec Escape +
  click outside, nav vers ancres sections
- `TerminalBar.tsx` — bar bottom fixed avec BrandMark + live time
  CH/CET (refresh 1s) + status dot pulse + ticker desktop + 2 CTAs
  toujours visibles
- `Hero.tsx` (S01) — headline 3 lignes "voix [cycle]" + top meta GPS
  + bottom 3-col (structure list + champ d'action + CTAs desktop)
- `Presentation.tsx` (S03) — 2-col header + 3-col body avec dl meta
  list + 2 paragraphes, Reveal sur scroll
- `Access.tsx` (S08) — main panel 2-col + 3-step grid + secondary
  dark panel pointant vers `/login`
- `Interlocutor.tsx` (S09) — left surface bg + photo placeholder +
  nom monumental ; right bio + 4 channels (tel/mail/whatsapp/linkedin)
- `LandingFooter.tsx` — 4-col nav grid + monumental wordmark + legal
  bottom strip
- `index.ts` — barrel des exports

### Animations ajoutées (`src/styles/animations.css`)

- `terminal-pulse` (1.4s opacity 1→0.3→1) pour les status dots
- `cycling-word` (0.5s opacity+translateY) pour le hero cycling
- `marquee` réutilisé (déjà existant)

### Pages / routes modifiées

- `src/pages/Home.tsx` complètement réécrit en assemblage landing.
  Compose TopProgress + IndexOverlay + Hero + Marquee + Presentation
  + Marquee + Access + Interlocutor + Marquee + LandingFooter +
  TerminalBar. Index button top-right ouvre l'overlay.
- `src/app/routes/index.tsx` — `<Route index element={<Home />} />`
  **sorti de PublicLayout** (full-bleed, TerminalBar = chrome). Login/
  Playground/Lab/Onboarding restent dans PublicLayout.

### i18n keys (parité FR/EN)

- ~70 nouvelles clés sous `landing.*` dans `fr.json` + `en.json`
- EN traduit en sommaire ce soir, à raffiner brand-voice demain
- Aucun FR hardcodé dans JSX (règle critique.md §7 respectée)

### Fix collatéral

- `src/features/inquiry/InquiryDrawer.tsx` — Record `SOURCE_NAV_KEY`
  manquait 3 clés (`jet`, `object-search`, `event-organize`) après
  ajout récent au type `InquirySource`. Pré-existant sur main, bloquait
  `pnpm validate:fast`. Mappées vers `account.nav.concierge` (fallback,
  à raffiner si nav admin évolue).

## Ce qui reste pour demain matin (avant client)

### HIGH — sections riches non-shippées

- **S02 Manifeste sticky** — 300vh sticky avec 3 phrases progressing
  selon scroll position. Lenis app-level + sticky peut se battre, à
  tester.
- **S04 Piliers** — 3 paragraphs avec IntersectionObserver sync sur
  pillar-marks (active pillar = pillar in view, autres opacity 0.3).
- **S05 Domaines** — 10 verticales liste hover → s05-preview swap.
  Owner direction : "applique notre mobile first" — sur mobile preview
  pourra être un drawer ou collapse.
- **S06 Expériences** — 1 large + 2 small exp-cards. **Sanity
  `experience` schema à créer** + données saison ouverte par Salva.
  Tonight les 3 cards sont en placeholder `[—]`.
- **S07 Manifeste 2** — "On ne parle pas de ce qu'on fait. On le fait,
  puis on se tait." + 4 mots bottom strip.

### MEDIUM — effets non-câblés

- **Scramble effect** sur h2 enter viewport (logic existait dans
  mockup JS, défini mais désactivé — `// (scramble disabled for now)`)
- **Index button hover** — micro-feedback lignes qui se réarrangent
  (tonight = simple hover bg-fg/text-bg)
- **Curseur custom** sur zones interactives (owner a dit "optionnel
  à discuter") — pas implémenté

### MEDIUM — Sanity backend

- Créer schéma `interlocutor` singleton avec photo Cloudinary
- Créer schéma `experience` document + array dans page-landing
- Créer/étendre `siteSettings` singleton pour ticker content + saison
- Migrer Salva data static → Sanity

### LOW — debt à régler

- **`max-w-[Xpx]` warnings IDE** — Tailwind 4 propose `max-w-N` canon
  (max-w-120 = 30rem = 480px). Lint warnings only, pas bloquant.
- **`z-[100]` / `z-[150]`** idem, peuvent passer en `z-100` / `z-150`.
- **Couleurs success token** — utilisé pour le dot mais le mockup
  voulait un vert `#00ff95` plus cyan-mint. Token `--color-success`
  donne `#00c853` qui est plus bluish. À voir si owner valide.

## Time check

- Branche `feat/landing-public` créée 23:00.
- 11 composants + i18n + routes + animations + fix collateral en
  ~25 min de Write/Edit.
- `pnpm validate:fast` vert exit 0 après 1 round de fixes (import sort
  + Record completeness + anchor-is-valid → button disabled).
- Dev server up sur :5174, /fr retourne 200.

## RELEASE CHECK

```
Commits depuis dernière release : 1 (en cours) sur feat/landing-public
Types : feat(landing) — scaffold publique spine
Validate pipeline : ✅ validate:fast exit 0
Recommandation : wait (continuer demain matin avec S02/S04/S05/S06/S07
  + Sanity + scramble + EN brand-voice trad, puis tag v0.6.0 quand
  livrable client est complet)
Raison : milestone partiel — la spine fait visuellement le job mais
  4 sections riches manquent. Mieux vaut un v0.6.0 cohérent demain
  qu'un v0.5.5 incomplet ce soir.
Next milestone : v0.6.0 "Landing publique complète" target 2026-05-13
  matin avant livraison client.
Push state : feat/landing-public sera pushé après ce journal +
  memory:index.
```

## Cross-refs

- Mockup référence : `docs/landing-mockup-v5.html` (était dans public/,
  déplacé pour éviter leak prod)
- Journal précédent : `sessions/2026-05-12-2200-preloader-baseline-addendum.md`
- Brand identity : `decisions/2026-05-12-brand-identity-saw-next.md`
- Mechanics utilisées : `creative-library/mechanics/morphing-typography.md`
  (cycling word) · à compléter avec scroll-driven-hero quand S02/S04
  arrivent
- Branche state : `feat/landing-public` créée depuis main, contient
  spine landing + InquiryDrawer fix
- Composants nouveaux : Hero, Presentation, Access, Interlocutor,
  LandingFooter, TerminalBar, IndexOverlay, TopProgress, Marquee,
  SectionTag
- Hooks nouveaux : useCyclingWord
- Routes modifiées : `/:locale/` index sort de PublicLayout
- Memory state : 102 entries après ce journal
