---
id: session-2026-06-02-0846
date: 2026-06-02
type: session
tags: [#sanity, #content, #client-specific]
scope: client-specific
status: active
---

# Session — Fix équipe écrasée par Sanity prod (matin)

Bug client urgent : la section Interlocutor du site affichait l'ancienne
équipe (Salvatore / Bokar / Harry) au lieu des 5 bons membres.

## Diagnostic

Repo correct partout (fallback `Interlocutor.tsx`, i18n `fr.json`,
fixture `sawnext-seed.json` = les 5 bons membres avec bonnes bios).
Le bug venait de la **base Sanity production** : 3 vieux docs
`teamMember` (team-salvatore/bokar/harry) d'un seed antérieur qui
écrasaient le fallback (le composant préfère Sanity quand des docs
existent). Confirmé via curl read-only sur l'API Sanity.

## Livré

- `studio/scripts/fix-team.js` + `pnpm sanity:fix-team[:dry]` — script
  chirurgical : supprime UNIQUEMENT les teamMember, recrée les 5 depuis
  la fixture. N'utilise PAS `--wipe` (qui détruirait le catalogue).
- Exécuté contre production avec token Editor : 3 supprimés, 5 créés.
  Vérifié en lecture : Valmont focal + réseau dans le bon ordre.
- Mergé sur main + push (0aade09).

## Friction écrite

`frictions/2026-06-02-sanity-stale-data-overrides-code.md` — réflexe à
garder : "X écrasé par Sanity" → lire la prod d'abord, fix chirurgical
par _type, jamais de wipe global.

## Sécurité

2 tokens collés dans le chat (1 Viewer raté + 1 Editor). Demandé à
l'owner de les révoquer sur sanity.io/manage. Prochaine fois : `.env.local`.

## Reste ouvert (hérité)

- Texte légal "Valmont" dans le formulaire d'accès (résidu persona) —
  toujours en attente OK owner.
- Le front lit via CDN Sanity (useCdn: true) — léger cache, l'équipe
  corrigée apparaît après expiration / hard refresh.
