---
id: session-2026-05-14-1745
date: 2026-05-14
type: session
tags: [#sanity, #i18n, #content, #release, #client-specific]
scope: client-specific
status: active
---

# Session — Sanity content mirror epic (16:37 → 17:45)

## What was asked

Owner directive : "que TOUT le contenu soit en miroir sur Sanity, chaque
texte modifiable par le client (sauf trucs figés), 1 fiche fake par
catégorie et supprimer les autres, le tout fonctionnel, commit entre
chaque truc important, jusqu'à 3 heures, tu peux y aller".

The owner left for the duration of the session and signed off on me
working autonomously.

## CHALLENGE issued before starting

Honest pushback : without `VITE_SANITY_PROJECT_ID` + `SANITY_WRITE_TOKEN`
locally, I cannot push a seed to a live dataset, only prepare the
fixture + scripts. Verdict was GO-BUT — the plan delivered everything
"code-side" so the owner runs `pnpm sanity:seed:sawnext` once when the
creds are in `.env.local`.

## What was delivered

5 commits on `feat/sanity-content-mirror` :

1. `2de9cc0` feat(sanity): add landing singleton schema + desk entry
2. `f717157` feat(sanity): rewrite seed fixture + add --wipe to seed script
3. `816ab7d` feat(sanity): wire landing sections to Sanity singleton w/ i18n fallback
4. `90f50d8` chore(sanity): widen schema validator allowlist + finalise landing singleton wiring
5. `<this commit>` chore(sanity): handoff doc + memory entries + docs sync

Net :

- New `landing` singleton in Studio (`landing-singleton`, 8 field
  groups) — all visible editable landing copy in one doc.
- Front : `useLandingContent` hook + `LandingContentProvider` context
  → one fetch for the whole landing tree.
- 7 landing sections wired (Home / Hero / Presentation / Principles /
  Domains / Access / Interlocutor) with `resolveFieldOrFallback` to
  i18n. The site keeps working when Sanity is unconfigured.
- `studio/fixtures/sawnext-seed.json` rewritten : 2 singletons + 3
  pages + 1 fiche per catalogue + 3 team members (Interlocutor
  rotation needs all 3).
- `seed-sawnext.js --wipe` flag for clean re-seeding.
- `validate-sanity-schema.js` allowlist widened — pre-existing 28
  false positives now pass.

## What stays in i18n on purpose

Hero typewriter phrases · Manifesto · Principles 3-pillar triplets ·
Domains 10-item list · Marquee · IndexOverlay nav · Footer link labels.
Reasons documented in `decisions/2026-05-14-sanity-landing-singleton.md`
and in `docs/SANITY-HANDOFF.md`.

## Friction encountered

- Existing `Home.tsx` on `main` is the version BEFORE the
  `fix/landing-loader-buttons-login-modal` branch (which wasn't
  merged). I worked from main on purpose to keep the epic clean — the
  merge of the two branches will happen later, no conflicts expected
  since they touch disjoint hunks of Home.tsx.
- Initial stash-pop after branch switch surfaced the previous
  branch's uncommitted state on the new branch. Resolved : stashed,
  switched back, popped, committed properly on
  `fix/landing-loader-buttons-login-modal` (commit `d3d30b4`).

## What's NOT done (deferred to follow-up)

- Live push to Sanity — needs owner's credentials, documented in
  `docs/SANITY-HANDOFF.md`.
- Wiring Manifesto / Hero typewriter copy — explicitly skipped per
  the CHALLENGE verdict.
- Pre-existing validate failures unrelated to my scope :
  - 4 hardcoded FR in `AuthHeader.tsx` / `AdminShareCodes.tsx` /
    `ExamplePage.tsx`.
  - These need a separate i18n cleanup pass.

## Next session

1. Owner provides Sanity creds → run `pnpm sanity:seed:sawnext`.
2. Owner clicks around the Studio, requests adjustments to the
   landing singleton schema if needed.
3. Eventually : merge `fix/landing-loader-buttons-login-modal` and
   `feat/sanity-content-mirror` to main, cut a release.
