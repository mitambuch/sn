---
id: session-2026-06-05-1451
date: 2026-06-05
type: session
tags: [#sanity, #content, #i18n, #client-specific]
scope: client-specific
status: active
---

# Session — Modèle de contenu catalogue (retour client Studio)

Branche `fix/catalogue-studio-content-model` (4 commits). **Non mergée.**

## Contexte

Retour client « j'ai QUE ça » sur le Studio : (1) Garde-Temps « Matériau
boîtier » pas traduisible ; (2) Private Retreats « Destinations » mono +
non-traduisibles ; (3) Itinéraire avec une heure au lieu de « Jour 1, Jour
2… » ; (4) question slug. Investigation → ces 4 points ont révélé une
divergence systémique (voir [[catalogue-triple-model-divergence]]).

## Livré (2 layers)

- **Layer 1** (`5964341`) — schémas Studio : caseMaterial → localeString,
  destinations → array de localeString, nouvel objet `itineraryDay` (sans
  heure, numérotation auto) ; GROQ mis à jour (L/LARR + drop time).
- **Layer 2** (`8ddfad9`) — rendu pages : timepiece material (i18n-sinon-brut)
  + JourneyDetail recalé sur le modèle Studio (itinéraire Jour N, transport,
  hébergement, party size) via type `JourneyDetailData` + pont mock ;
  `destinations` élargi string|array normalisé partout ; i18n FR/EN ajouté.
  Au passage : **crash JourneyDetail sur fiche Sanity corrigé**.

## Réponse slug (owner)

Slug = identifiant unique partagé FR/EN, l'URL est la même dans les deux langues
(seul le contenu change). Standard, bon SEO. Pas de changement. Localiser les
slugs = possible mais déconseillé (complexité routing).

## Décisions

- Approche **additive/pont** plutôt que réécriture des types partagés —
  les types domaine sont consommés par mocks + cards + admin in-app, une
  réécriture ripple trop. Localisé au detail + 1 type dédié.
- Owner a choisi « caler la page voyage sur le Studio » (drop legs/origin/
  inclusions mock) plutôt qu'ajouter ces champs à Sanity.

## Reste ouvert

- Autres modules (property/artwork/event/conciergeService/article) : mêmes
  ponts à faire. Listes/cards + admin in-app encore sur l'ancien modèle.
- Pour prendre effet : merge + push (app redéploie → pages) **et redéploiement
  du Sanity Studio** (pour que les nouveaux champs apparaissent à l'édition).

## Release

`pnpm validate` vert (519 tests, coverage au-dessus du ratchet, build).
v1.3.0 (tunnel d'accès) toujours en attente du « go » owner ; ce lot contenu
peut soit rejoindre v1.3.0, soit partir en v1.3.x à part.
