---
id: 2026-04-20-1511-dispatch-sensitivity-shipped
date: 2026-04-20
type: session
tags: [#workflow, #perf, #release, #template, #active]
scope: template
status: active
---

# Session — Dispatch auto-routing fix (v6.8.0)

## Context

Owner observation : "hier quand je travaillais j'ai vu que tu utilisais
pas le système de dispatch automatiquement". Diagnosed two concrete
failures this session :

1. **Classifier regex too narrow** — "patch GAFHA", "bump deps", "polish
   Studio UX", "implémente", "extract", "nettoie imports" all fell
   through to SKIP (no `[DISPATCH_CLASS: ...]` hint injected).
2. **JS `\b` + accented chars** — `\bécri`, `\b[eé]cri`, `\bmets? à`
   all fail at start-of-string because JS `\b` is ASCII-only even
   under `u` flag (needs `v` flag for Unicode boundaries). Silent
   misclassification whenever a French verb started with é/à.

Plus a meta-issue : even when the classifier was right (e.g. classifying
a cross-cutting refactor as R2 correctly), Claude wasn't self-announcing
the classification → invisible decision-making → no trail for the owner
to correct or the Stop hook to audit.

## What shipped in v6.8.0

### Commit 1 — perf(dispatch): classifier broadening + 44-case tests
- R0 patterns : +patche?, +propage?, +applique?, +nettoie, +clean up,
  +rattrape, +catch up, +bump|upgrade deps, +pnpm docs:sync, +supprime
  import, +patch the (site|project), +tidy up. 13 new signals.
- R1 patterns : +implémente, +monte, +extract, +refactor local, widened
  ajoute?r? (was ajouter only). 5 new signals.
- Fixed broken "mets à jour les deps" regex (`met(s| à)? ?jour` →
  `mets?\s+(?:à|a)\s+jour`).
- Replaced every leading `\b[accent]` with `(?:^|\s)[accent]` —
  works regardless of the Unicode-boundary quirk.
- Added `u` flag to every pattern.
- New `scripts/__tests__/classify-task.test.js` — 44 real-phrasing
  cases grouped R0/R1/R2/SKIP + precedence tests + confidence floor.

### Commit 2 — feat(dispatch): critical.md §8 mandatory announcement
- §8 rewritten : the classification line is the first non-greeting
  content of every tool-using turn. NOT optional. NOT conditional on
  the `[DISPATCH_CLASS: ...]` hint. Format :
  `Classification: R0/R1/R2/SKIP — <one-phrase why>`.
- Explicit decoupling from §12 (hook contract) : the hint is a
  heuristic filet de sécurité, the announcement is the discipline.
- Default on uncertainty : R2 + say so. No silent work.

Release : https://github.com/mitambuch/steaksoap/releases/tag/v6.8.0

## Frictions

1. **Unicode-boundary bug** took 3 iterations to diagnose. Confusing
   output because `\b` under `u` flag looks like it should work per
   MDN summary — actual behavior requires `v` flag (newer). Once
   confirmed via direct Node regex tests, fix was mechanical.
2. **Initial test suite** had 4 failures across 3 real bugs (mets à
   jour, écris un test, ajoute un champ) — useful signal, the suite
   earned its keep by surfacing them before release.

## Decisions taken

- **Announcement > hook-trust** : made the classification announcement
  the primary enforcement mechanism. Hook remains as a filet de
  sécurité / strong prior, but the discipline no longer depends on it.
- **Drop leading `\b` before accented chars** instead of switching
  everything to the `v` flag — `v` support varies across Node
  versions, `(?:^|\s)` is universal.
- **R2 on uncertainty** — when the rule could have said "SKIP on
  uncertainty" for safety, chose R2 so the CHALLENGE block (§11)
  kicks in and forces explicit reasoning.

## RELEASE CHECK

```
RELEASE CHECK:
- Commits depuis v6.8.0 : 0 (this session's commits are in the release)
- Recommandation : wait
- Raison : v6.8.0 just shipped. Housekeeping remains (this journal).
  Behavior change will be visible on the NEXT turn — I'll be required
  to announce Classification at the top of every tool-using response.
- Next milestone : feedback terrain sur le new discipline, OR HDVA /
  client-project patch after dogfooding the announcement a few turns.
```
