---
id: session-2026-06-08-1537
date: 2026-06-08
type: session
tags: [#sanity, #i18n, #content, #client-specific]
scope: client-specific
status: active
---

# Session — Triage salve client « Voyages » + fix bug listes multilingues

Branche `fix/catalogue-studio-content-model` (reprise — la branche de la semaine
dernière). Nouveau commit `da49b75`. **Non mergée.**

## Contexte

Owner transmet un lot de 8 retours pris par son client (SAW Next) en utilisant le
Studio. Demande : trier, mettre de côté ce qui n'est pas clair, dire quoi
renvoyer au client. Point clé découvert : **la moitié du lot est déjà corrigée la
semaine dernière mais jamais livrée** (branche non mergée + Studio non
redéployé) → le client regarde l'ancien état.

## Triage des 8 points

**Déjà fait (à livrer)** : itinéraire par jours (`itineraryDay`), transport +
hébergement FR/EN (`array localeString`), destinations FR/EN + multiples.

**Clair + nouveau** :
- Défilement profils (landing `Interlocutor.tsx`) trop rapide — `SLIDE_DURATION_MS`
  8000 → ~15000 (contredit une ancienne direction owner « 8s », signalé).
- Destinations centralisées/réutilisables/filtrables = le client veut un vrai
  **type de doc `destination`** référencé (taxonomie i18n-sanity correcte). Plus
  gros que le quick fix array-localeString. « Filtrer » = « à terme ».
- Bug listes multilingues « Untitled » → **fixé** ce tour, voir
  [[locale-object-no-preview-in-arrays]].

**Décision owner / à clarifier client** :
- Bouton « Voir Valmont » dynamique (point 5) — **contredit un choix délibéré** :
  Valmont = contact opérationnel unique, canaux `FOCAL_CHANNELS` non-overridables
  « pour sécurité ». Le client veut per-profil (email perso OU générique
  `info@saw-next.ch` + objet pré-rempli). → décision brand/sécurité owner.
- Slug par langue (point 6) — aujourd'hui **un seul slug partagé** FR/EN (source
  `title.fr`). Déconseillé la semaine dernière ; client réinsiste pour le SEO.
  Recommandation : différer post-MVP (chantier routing/hreflang).

## Livré ce tour

Fix bug listes (point 7) : `preview` ajouté sur `localeString` + `localeText`
(title FR + statut EN), pour que les items de liste ne soient plus « Untitled ».
Studio-only, gates verts. Détail + caveat : [[locale-object-no-preview-in-arrays]].

## Reste ouvert / décision owner

1. **Livraison** : merger `fix/catalogue-studio-content-model` **ET**
   `feat/member-holding-state` sur main + push (app redeploy) + **redéployer le
   Studio**. Les deux branches sont cohérentes ensemble (Salva édite un Studio
   amélioré ; les membres voient « en préparation »). Conflit probable mineur sur
   `locales/*.json` (clés `account.*`) à résoudre au merge. → go owner (outward).
2. Décisions points 5 (bouton Valmont) et 6 (slug) en attente.
3. Quick win point 4 (défilement profils) pas encore codé.
4. Chantier point 1bis (type doc `destination`) à planifier.

## Release

v1.3.0 toujours en attente. Ce fix Studio + la livraison des deux branches =
candidats au prochain cut.
