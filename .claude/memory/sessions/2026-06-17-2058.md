---
id: session-2026-06-17-2058
date: 2026-06-17
type: session
tags: [#routing, #sanity, #security, #content, #client-specific]
scope: client-specific
status: active
---

# Session — 3 fix /presentation + fiche produit publique

Branche `feat/public-fiche-presentation-fixes` (non mergée — attend « push »).
3 commits sur `a878022`.

## Livré

1. **fix Boudry** (`9aab1a4`) — `/presentation` affichait « Genève · Suisse »
   ET des GPS `46°12′N · 06°09′E` (= littéralement Genève). Maison domiciliée
   Boudry. → `qr.hero.m3Value` = Boudry, `qr.hero.gps` = `46°57′N · 06°50′E`
   (fr/en/es). Autres « Genève » laissés (escrow notaire, gala ONU = factuel).

2. **fix PDF blanc** (`08b1420`) — client signalait PDF tout blanc au
   téléchargement. Cause : `PrintSheet` en `print:fixed print:inset-0` → page
   blanche sur Safari/iOS + ignore les marges @page. Le commentaire d'index.css
   décrivait pourtant `hidden print:block` — le composant avait divergé. → flux
   normal `print:block`, marges via `@page { margin: 16mm }`. Voir friction
   liée : un `print:fixed` ne s'imprime pas de façon fiable cross-browser.

3. **feat fiche publique** (`61566f3`) — voir [[public-catalogue-fiche-route]].
   Route `/c/:type/:id` lecture seule pour les docs `visibility: public`,
   ouverte depuis le teaser 08.A. Hook gate-aware + action Netlify `publicFiche`
   + GROQ durci. Vignettes réelles → `<Link>`, cadenas inchangés.

## Revue adversariale (2 agents avant livraison)

- **Sécurité** : pas de bypass d'accès (clause `visibility=="public"` +
  validation `isModuleKey`/`DOC_ID_RE` avant interpolation = surface injection
  fermée). MAIS a trouvé une **fuite de données** : le `...` brut de la
  projection share embarquait prix/provenance/specsheets dans le payload public.
  → corrigé : projection **whitelist** `PUBLIC_FICHE_PROJECTION` sans `...`.
  Pattern : [[public-route-whitelist-projection]].
- **Correctness** : tout OK (trace prod gate, mock dev, états initiaux, parité
  i18n fr/en/es, round-trip route 7 types, PDF). 1 point LOW DEV-only
  (fiche stale sur nav SPA fiche→fiche sans remount) — inatteignable en prod
  (aucun lien fiche→fiche), non corrigé volontairement (surgical).

## État ship / à tester en ligne (post-deploy)

- **Pas mergé / pas poussé.** La fonction Netlify `publicFiche` déploie AVEC le
  site (live ensemble) — sinon fiche vide en prod gate.
- Tester : tagger un event `public` dans Sanity → cliquer la vignette accueil →
  fiche s'ouvre (image, titre, date/lieu, CTA). Vérifier qu'aucun prix/provenance
  n'apparaît dans l'onglet Network. PDF /presentation sur Safari iOS.

## Validation

`pnpm validate` : lint + typecheck (+ functions + studio) + 553 tests (dont
`PublicFiche.test`) + coverage verts. Build OK avec `VITE_APP_URL` (échoue en
sandbox sans `.env.local` = faux-positif connu [[2026-06-17-release-from-sandbox-gates]]).
