---
id: session-2026-06-04-1119
date: 2026-06-04
type: session
tags: [#auth, #security, #testing, #client-specific]
scope: client-specific
status: active
---

# Session — Refonte du tunnel d'accès (mot de passe, un seul flux)

Branche `feat/auth-password-tunnel`, commit `2d2885f`. **Non mergé** — en
attente de tests live owner + réglages dashboard Supabase.

## Contexte

L'owner : le tunnel de connexion est « brouillon », il ne le comprend pas
lui-même. A demandé un tunnel « PUTAIN simple » et « blindé à 100% ». Diagnostic
avant code (R2) → 3 défauts réels identifiés, pas un ressenti.

## Diagnostic (3 bugs, vraies causes)

1. Email d'acceptation pointe vers `sawnext.ch` (faux) au lieu de `saw-next.ch`
   (Resend + CI). → tombe sur Vercel. Owner a tranché le domaine canonique.
2. Aucun système de mot de passe : login 100 % lien magique, mode mot de passe
   mort, double-email + double-code à l'onboarding.
3. Deux UIs de login divergentes (page `Login.tsx` + `LoginModal.tsx`).

## Livré

- **AuthContext** : `registerWithCode` (verify → signUp password → redeem),
  `requestPasswordReset`, `updatePassword`. Retiré magic-link/redeem/confirm.
  Tous les redirects email via `env.APP_URL`, jamais `window.location.origin`.
- **LoginPanel** (nouveau) : UI + logique partagées. `Login.tsx` (page) et
  `LoginModal.tsx` (overlay) sont des coques fines → plus de divergence
  possible. Voir pattern [[shared-panel-two-shells]].
- **ResetPassword** + route `/reset-password` + `ROUTES.RESET_PASSWORD`.
- **Onboarding** réduit à un écran de bienvenue (code déjà consommé).
- **AccessRequestModal** « J'ai un code » → `/login` première connexion,
  pré-rempli ; accepte le code brut 8 car de l'email.
- **Migration 0021** (toujours pending) : `sawnext.ch` → `saw-next.ch` + copie
  clarifiée.
- **vitest** : `testTimeout` 5s → 15s (flake gate, voir
  [[vitest-coverage-testtimeout-flake]]).
- Tests : AuthContext mis à jour, LoginPanel (toutes branches), ResetPassword.

## Validation

`pnpm validate` vert sur tout sauf le build qui exige `VITE_APP_URL` en local
(CI l'injecte). Confirmé : `VITE_APP_URL=https://saw-next.ch pnpm build` → OK.
Gates ✓, 519 tests ✓, coverage 48.18 % lines / 32.52 % branches (au-dessus du
ratchet 47/32).

## Reste à faire (OWNER — bloquant pour le live)

1. Supabase Auth → **Confirm email = OFF** (sinon 2e email + code non consommé).
2. Supabase Auth → **Site URL = `https://saw-next.ch`** + Redirect URLs
   `https://saw-next.ch/**`.
3. **`VITE_APP_URL=https://saw-next.ch`** dans l'env de déploiement prod.
4. Appliquer la migration 0021 corrigée si pas déjà appliquée.
5. Tester de bout en bout : demande → accepté → email (bon domaine) → login
   première connexion → mot de passe → compte ; puis reconnexion ; puis reset.

## Déploiement & validation live (suite, ~15h30)

- Owner a fait les 4 réglages (Confirm email OFF, Site URL, VITE_APP_URL,
  migration 0021). `.env.local` créé localement (gitignored) pour que
  `pnpm validate` passe en local (build exige VITE_APP_URL) → validate EXIT 0.
- **Mergé + poussé** sur main (`71bee74`), branche supprimée, déployé.
- **Les DEUX domaines servent l'app** : `saw-next.ch` ET `sawnext.ch` pointent
  sur le même déploiement (vérifié via WebFetch). Les liens email (saw-next.ch)
  marchent ; l'owner navigue sur sawnext.ch. Ne jamais supprimer saw-next.ch
  (il est dans les liens d'email).
- **« Voile noir » signalé puis résolu** = cache navigateur post-déploiement,
  pas un bug. Repro propre = dashboard nickel. Voir [[post-deploy-stale-cache]].
- **Validé live par l'owner** : reconnexion email+mot de passe OK, première
  connexion (email → code → mot de passe) OK. Tunnel fonctionnel de bout en bout.

## Suivi déféré (non bloquant)

- **Emails Supabase Auth** (reset password) partent encore de `@supabase.io`.
  Pour les brancher sur `saw-next.ch` : Supabase → Authentication → Emails →
  SMTP Settings → Custom SMTP via Resend (host `smtp.resend.com`, port 465,
  user `resend`, password = clé API Resend, sender `noreply@saw-next.ch`).
  **Déféré par l'owner** (perçu comme payant ; en réalité palier gratuit Resend
  suffit). Cosmétique — le reset fonctionne déjà. Template FR à rédiger aussi.

## Release

Tunnel validé live de bout en bout → **v1.3.0 justifiée**. En attente du « go »
explicite de l'owner pour couper (release = tag + changelog + push, outward-facing).
