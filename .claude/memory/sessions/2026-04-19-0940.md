---
id: session-2026-04-19-0940
date: 2026-04-19
type: session
tags: [#workflow, #memory, #milestone, #template, #active]
scope: template
status: active
---

# Session — Anti-complaisance, intent routing, méta-stack optimization

Session matin sur une demande de fond : stop being complacent + optimiser
le repo sans perdre de valeur. 5 commits sur `feat/anti-complaisance`,
tous validés `pnpm validate:fast`, aucun merge/push — on attend la
vérification empirique sur 3-4 sessions avant release.

## Contexte

Owner a cadré le problème : Claude défaulte vers la validation même
quand la prémisse est faible. Sans pushback technique soutenu, dette
invisible accumulée. Demande initiale : "jury system" multi-critères.
**J'ai contesté** — sur-ingéniéré, redondant avec rules existantes. Owner
a accepté la version minimale. Premier signe que l'anti-complaisance
marche : je corrige sa proposition au lieu de l'implémenter par
complaisance.

## Ce qui a shippé (5 commits, branche `feat/anti-complaisance`)

### `99b8b3e` — anti-complaisance rule + CHALLENGE block
- Nouvelle règle always-loaded `.claude/rules/anti-complaisance.md` :
  contrat, phrases interdites ("super idée", "tu as raison", "parfait"),
  phrases attendues ("je conteste X parce que Y"), bloc CHALLENGE 3
  lignes avant tout R2.
- `critical.md` §10 + §11 pointent vers la règle + format CHALLENGE.
- CLAUDE.md + `scripts/hooks/session-start.js` : registre always-loaded
  resynchro.
- `decisions/2026-04-19-anti-complaisance.md` : ADR avec alternatives
  rejetées (jury complet, hook bloquant).

### `485f10c` — intent routing
- Nouvelle règle always-loaded `.claude/rules/intent-routing.md`
  (~35 entrées) : mapping intention naturelle → `/xxx` + agents.
- Owner ne tape JAMAIS `/`, il dit "on ajoute une page" → auto-invoke
  `/new-page`. Les 19 commands "dormantes" + 2 agents "morts" (tester,
  reviewer) réactivés sans suppression.
- Revient sur l'idée de purger des commands — lecture du contenu a
  montré que toutes contiennent des workflows/checklists load-bearing.

### `8072bc6` — path-trigger i18n-sanity.md
- Passe de `paths: ["**"]` à `src/**/*.tsx · src/locales/** · src/lib/i18n*
  · src/lib/sanity* · studio/**`.
- Gain ~6.8 k tokens par turn hors contexte content. Safety net :
  `critical.md` §7 + `scripts/validate-i18n.js`.

### `4d6943b` — path-trigger dispatch.md + inline mechanic
- Dispatch.md path-triggered sur meta-files (`.claude/agents/**`, etc.).
- Le mécanisme d'invocation critique (Agent tool + model override Haiku/
  Sonnet) inliné dans `critical.md` §8 — dispatch reste fiable sans la
  règle complète loaded à chaque turn.
- Gain net ~6.4 k tokens/turn (6.8 k retiré − 0.4 k ajouté à §8).

### `2a4a259` — memory discipline hardened
- `memory-protocol.md` : sections "Trigger phrases" concrètes pour
  `feedback/`, `patterns/`, `frictions/` + session-wrap audit 3 questions
  Q/A obligatoires avant journal.
- `scripts/hooks/stop.js` : nudge systématique avec les 3 questions +
  counts actuels des folders.
- `feedback/2026-04-19-stop-complaisance.md` : **première entrée feedback
  de tout le repo** (30 jours de retard rattrapé). Applique la règle à
  elle-même.
- `decisions/2026-04-19-memory-discipline-hardening.md` : ADR.

## Méta-observations

### Always-loaded count : 9 → 8 après P6
Avant session : 9 règles always-loaded (dispatch + i18n-sanity +
time-awareness + critical + principles + releases + memory-protocol +
workflow + nouveau anti-complaisance).

Après : 8 always-loaded (dispatch et i18n-sanity path-triggered, intent-
routing ajouté). Net gain : passage de ~35 k tokens/turn always-loaded à
~28 k, avec **meilleure** couverture fonctionnelle (intent routing
réactive 19 commands dormantes).

### Démonstration anti-complaisance en live
Au moins 3 fois pendant cette session :
1. **Contesté la demande "jury system"** — trop complexe, inutile.
2. **Contesté l'objectif "repo qui trend sur GitHub"** — vanitaire, pas
   aligné avec la réalité du projet.
3. **Inversé ma propre position P1** après que l'owner a pointé qu'il ne
   tape jamais `/xxx` — admis "j'avais tort", refondé le plan.

Sans la règle anti-complaisance, j'aurais validé par défaut chaque point.
Preuve que la règle a déjà changé le comportement dans la session qui la
crée.

### Applied the CHALLENGE format live
Format émis 3+ fois (Phase C, Phase B, P6 refinement). Verdict mix
GO/GO-BUT/NO-GO. A tranché proprement chaque fois.

## RELEASE CHECK

```
RELEASE CHECK:
- Commits depuis dernière release (v6.3.0) : 5 (cette branche)
- Types : feat(3) · refactor(2)
- Recommandation : WAIT
- Raison : changements comportementaux fondamentaux (anti-complaisance +
  intent routing + memory discipline) non empiriquement validés. La
  règle `anti-complaisance.md` spécifie 3-4 sessions de test avant
  durcissement/relâchement. Release prématurée = shipper une règle
  non-testée = reproduire l'erreur du v6.0 dispatcher (dont le ROI n'est
  toujours pas mesuré).
- Next milestone : v6.4.0 "Behavioral layer" quand :
  (a) 3-4 sessions confirment que je ne re-deviens pas complaisant
  (b) Intent routing déclenche sans faux-positifs notables
  (c) Memory discipline produit au moins 2-3 entrées feedback/pattern
      additionnelles de façon naturelle
  Cible estimée : 2026-04-22 à 2026-04-26.
```

## Prochaines étapes (whenever owner wants)

- Tester la branche `feat/anti-complaisance` sur une tâche réelle
  (bug fix ou nouvelle feature) — vérifier que :
  1. Je contestes sans théâtre
  2. L'intent routing déclenche bien sur "on ajoute une X"
  3. dispatch.md path-trigger ne casse pas le dispatch
  4. Le stop hook émet bien le nudge memory discipline

- Après 3-4 sessions OK → merger sur main, cut v6.4.0 "Behavioral
  hardening".

- Si dérive observée → ajustements ciblés sur les 3 règles.

## Ce qu'on N'a pas fait

- Aucune purge de command / agent (renversement après lecture du
  contenu — tout est load-bearing via intent routing).
- Aucun merge sur main, aucun push.
- Aucune release.
- Audit ROI du dispatcher v6.0 — toujours pas mesuré empiriquement.
  À faire dans une session dédiée sur des logs réels.

## Addendum (~10:10) — `ca23f19` feat(rules): conducteur/navigateur

Deuxième feedback owner juste avant la pause : le bloc `ACTION HUMAINE
REQUISE` était trop technique et trop fréquent, l'owner ne pouvait pas
toujours juger entre les options offertes. Correctif immédiat :

- Nouvelle entrée `feedback/2026-04-19-decide-for-me.md` (seconde
  entrée `feedback/` écrite **sans nudge** — la règle hardened s'est
  auto-appliquée, preuve de concept).
- `critical.md` §5, `workflow.md` §User mobilization, `CLAUDE.md` §User
  mobilization : reshape en règle **conducteur/navigateur**. Par défaut
  Claude tranche seul les choix techniques. `ACTION HUMAINE REQUISE`
  réservé à taste/access/business/irréversible/info-externe.
- Format simplifié (6 lignes, zéro jargon, toujours une recommandation
  claire, conséquences en langage simple OUI/NON).
- `anti-complaisance.md` : ajout §Corollaire "Décide pour moi" pour
  éviter contradiction apparente avec la règle anti-complaisance.

Branche `feat/anti-complaisance` passe de 5 à **7 commits** (+ ce
session addendum = 8 au commit près).

## Cross-refs

- Decisions :
  - `decisions/2026-04-19-anti-complaisance.md`
  - `decisions/2026-04-19-repo-objective.md`
  - `decisions/2026-04-19-intent-routing.md`
  - `decisions/2026-04-19-memory-discipline-hardening.md`
- Feedback (2 entrées) :
  - `feedback/2026-04-19-stop-complaisance.md`
  - `feedback/2026-04-19-decide-for-me.md`
- Branche : `feat/anti-complaisance` (7 commits + session journal,
  unmerged)
- Session précédente : `sessions/2026-04-19-0305.md` (v6.0 plan 5/5)
