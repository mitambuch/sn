---
id: session-2026-04-19-1644
date: 2026-04-19
type: session
tags: [#workflow, #i18n, #release, #template, #active]
scope: template
status: active
---

# Session — ChatGPT R2 audit (2e round) + application directe des fixes

Suite immédiate de la session 1610. L'owner a relancé ChatGPT pour un
audit plus profond (R2). Cette fois ChatGPT **a modifié le repo lui-
même** (24 fichiers, +482 / −314) au lieu de lister des recos.

## Ce que ChatGPT R2 a corrigé (appliqué et mergé)

**Commits** :
- `3e80108 fix(v6.5): apply ChatGPT R2 audit fixes`
- `1ee1e93 merge(fix): ChatGPT R2 audit fixes — Sanity gate + i18n wiring`

**Fixes P0 appliqués** :

1. **Sanity gate strict** — `scripts/validate-sanity-content.js` gagne
   `--required` / `SANITY_CONTENT_REQUIRED=1` qui fail au lieu de skip
   quand env manquant ou Sanity inatteignable. Nouveau script
   `pnpm validate:sanity-content:required` pour gate pré-prod.
2. **Header i18n-aware** — `src/components/layout/Header.tsx` : nav
   avec `labelKey` + `t()`, URLs préfixées par locale, LanguageSwitcher
   wiré avec navigation preserving-locale.
3. **LanguageSwitcher controlled-mode** — `currentLocale` +
   `onLocaleChange` props, test dédié nouveau.
4. **Dataset staging par défaut** — studio config + env examples
   changent de `production` vers `staging` (sécurité).
5. **CSP Sanity** — `vercel.json` + `netlify.toml` étendent connect-src
   pour `apicdn.sanity.io` + preview.
6. **Studio .env loading** — `studio/scripts/push.js` charge maintenant
   `studio/.env.local` et `.env.local`.
7. **Docs** — SETUP, DEPENDENCIES, env-vars, BOOTSTRAP, wire-content,
   i18n-sanity mises à jour pour refléter le flow staging-first + gate
   strict + switcher.

Tests : +1 nouveau (LanguageSwitcher), Header test élargi.

## Points du verdict ChatGPT R2 à noter

**AGREE** :
- Always-loaded ~10 443 tokens (calcul identique au mien après fix).
- CLAUDE.md ~1 935 tokens.
- Dispatch "muscle dormant" — à mettre en probation via hook session-wrap.
- `/context` = gimmick dangereux si vu comme source de vérité runtime.
  Clarifier : c'est un external brief one-shot, pas un runtime truth.

**DISAGREE partiel** :
- ChatGPT dit "23 992 tokens total rules + CLAUDE.md" — ça inclut les
  22 rules mais seulement 8 sont always-loaded (~10k). Les 14
  path-triggered ne se chargent que selon contexte, pas permanent. Le
  chiffre gonfle artificiellement.

**DISAGREE franc** :
- **Modèles IDs** : ChatGPT écrit "je vois claude-opus-4-1-20250805
  [...] pas les IDs 4.7/4.6/4.5". Faux dans ce contexte. Le system
  prompt Claude Code actuel liste explicitement : Opus 4.7 =
  `claude-opus-4-7`, Sonnet 4.6 = `claude-sonnet-4-6`, Haiku 4.5 =
  `claude-haiku-4-5-20251001`. ChatGPT a probablement consulté des
  docs Anthropic publiques non à jour ou cache. Garder les noms
  actuels dans `dispatch.md` et `critical.md`. **Pattern général** :
  ne pas blindly accepter les assertions ChatGPT — valider avec les
  docs internes / system prompt.

## Discipline capturée pour demain (ChatGPT R2 reco valide)

L'owner se limite demain à **10 commandes** seulement (client critical
path) :

```
/init /brief /wire-content /translate /sync-content
/pre-delivery /fix /review /test /context
```

Les autres (`/legal`, `/changelog-client`, `/morning-brief`,
`/delegate`, `/integrate`, `/discover`, `/connect`,
`/install-extension`, `/migrate`) sont parkées hors chemin critique.

## RELEASE CHECK (mise à jour post-R2)

```
Commits depuis v6.4.0 : 8 (main ahead of origin)
  ... (les 6 précédents) ...
  3e80108  fix(v6.5): apply ChatGPT R2 audit fixes
  1ee1e93  merge(fix): ChatGPT R2 audit fixes

Types : feat(1) · fix(2) · chore(3) · merge(3)
Recommandation : release v6.5.0 "Bootstrap & Context Bible + Sanity Strict Gate"
Raison : bundle audité 2× par ChatGPT, corrections appliquées en 2
         rounds, validate vert, husky merge-aware validé 3× aujourd'hui.
         Minor bump 6.4.0 → 6.5.0.
Push state : local, pas poussé à origin/main.
Next milestone : attente go owner explicite pour pnpm release:minor.
```

## Cross-refs

- Session précédente : `sessions/2026-04-19-1610.md` (même arc)
- Pattern cross-audit : `decisions/2026-04-19-llm-cross-audit-pattern.md`
- Branche ChatGPT-R2 : `fix/chatgpt-r2-audit-fixes` (mergée et supprimée)
- Plan complet : `~/.claude/plans/top-maintenant-qu-on-mis-wiggly-russell.md`
