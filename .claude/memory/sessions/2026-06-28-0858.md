---
id: session-2026-06-28-0858
date: 2026-06-28
type: session
tags: [#content, #routing, #client-specific, #design]
scope: client-specific
status: active
---

# Session — THE ODYSSEY content swap + Azimut Grande Trideck vessel section, shipped

The Hidden Shore pitch page rewritten for the revised client brief, plus a yacht
section, shipped to prod. Branch `feat/the-odyssey-content` → PR → merged to main.

## What changed

1. **Content swap (`ab74367`)** — `The Hidden Shore` (1-day Portimão) → **THE
   ODYSSEY** (3-day Cascais → Algarve voyage, 1–3 July). Owner pasted FR copy;
   chose **all-English** (matches the anglophone recipient + the page's existing
   English chrome). Rewrote [src/data/hiddenShore.ts](../../../src/data/hiddenShore.ts):
   cover, concept, 6 story chapters, facts, 4 included groups, closing. Dropped
   the unused `meta`/`marquee` fields.
2. **A/B module kept but DORMANT** — owner: "garde le module A/B mais cache-le au
   cas où on rajoute une version". `timeline` emptied, `<Day/>` gated off via
   `const SHOW_DAY: boolean = false` in [TheHiddenShore.tsx](../../../src/pages/TheHiddenShore.tsx).
   Refill the data + flip the flag to surface a second version. Types/optionLabels
   retained.
3. **Vessel section (`ea3b000`)** — client sent the Azimut Grande Trideck brochure.
   Added a `vessel` export + `Vessel` component (SectionHead + dl grid pattern,
   placed Story → Vessel → Included): name, lead, designer credits (Alberto
   Mancini / m2atelier / Azimut Yachts), 6 specs from the spec sheet, external CTA
   to the official model page so the recipient sees the photos.
   Link found via web search: `https://azimutyachts.com/en/grande-series/grande-trideck/`.
4. **Docstring fix (`cc3cb7e`)** — Story said "eight chapters", now 6.

## Decisions (decided solo, informed owner)

- **Route slug kept** `/the-hidden-shore` despite the title becoming THE ODYSSEY —
  the link was already shared; changing the URL would 404 it. Only the displayed
  title changed.
- **Vessel section added alongside the Included "The Yacht" group** (not a
  replacement) — different altitude: Vessel = boat identity + photos link,
  Included/The Yacht = charter scope. Mild overlap accepted (pitch docs repeat key
  selling points).
- Cover video still `portimao.mp4` (Algarve coastline — still fits). Flagged to
  owner as optional follow-up.

## Validation

`pnpm validate` green (VITE_APP_URL set to dodge the sandbox build false-positive
[[2026-06-17-release-from-sandbox-gates]]). **584/584 tests**, coverage thresholds
held even with `<Day/>` now uncovered (dormant) — global headroom absorbed it.
Hit the known [[2026-06-10-memory-index-check-date-drift]] gate once (day rolled to
the 28th) → `pnpm memory:index` fixed it.

## Next

- Owner to eyeball the EN translation in prod (esp. The Arrival 12/40 guests line,
  The Signature Moment / Julien Durix).
- A second journey version → refill `timeline`/`optionLabels`, flip `SHOW_DAY`.
