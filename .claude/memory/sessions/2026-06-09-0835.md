---
id: session-2026-06-09-0835
date: 2026-06-09
type: session
tags: [#i18n, #sanity, #content, #client-specific]
scope: client-specific
status: active
---

# Session — Espagnol comme 3ᵉ locale (infra complète, contenu data différé)

Owner : « traduire le site en espagnol et tout mettre en miroir sur Sanity,
lance-toi ». CHALLENGE émis (R2) → verdict GO-BUT : j'ai livré la moitié
infra (mon métier, zéro risque) ; la moitié données éditoriales Sanity est
owner-bloquée (token + voix de marque) et reste à décider.

## Fait (commit 8fdf90e, branche `feat/spanish-locale`)

- `SUPPORTED_LOCALES = ['fr','en','es']` + ressource es.
- `src/locales/es.json` — traduction complète des 1565 lignes (registre
  *usted*, sans points d'exclamation). Délégué à un worker Sonnet, **parité
  exacte vérifiée** : 1167/1167 clés feuilles, 0 manquante / 0 en trop.
- Studio : champ `es` sur localeString/Text/RichText (warning) + 3ᵉ onglet ES
  dans `LocaleTabsInput`.
- **GROQ** (`sanityQueries.ts`) : branche `$locale == "es"` ajoutée à chaque
  helper (L/LPT/LARR/L_LABEL/L_SELF + le select inline du share). C'était le
  vrai blocage — sans ça, le catalogue restait FR même en mode ES. Partagé
  avec la gate Netlify → corrige client ET serveur.
- Fix du collapse binaire `=== 'en' ? 'en' : 'fr'` dans useSanityCollection,
  useSanityItem, SharePage, Home, et `catalogue.mts` → dérivation 3-voies.
- Types Locale élargis (sanityGate, catalogue.mts) ; props switcher Home
  fr|en → Locale ; SeoHead og:locale es_ES.
- `pnpm validate` **vert** (lint, typecheck, studio:typecheck, i18n,
  sanity-schema, coverage, build).

Détail réutilisable extrait dans [[adding-a-locale-audit-binary-collapses]].

## Différé volontairement (décisions owner)

1. **Contenu éditorial ES dans Sanity** — le « miroir » au sens données.
   Besoin de `SANITY_WRITE_TOKEN` (absent en local) + d'un choix qualité :
   machine-translate-puis-review (rapide, risque voix) vs Salva remplit à la
   main via le nouvel onglet ES (lent, qualité humaine). En attente réponse.
   Cf garde-fou ajouté dans `validate-sanity-content.js` (ES pas encore requis).
2. **Préférence de langue compte = ES** — enum Postgres `user_locale ('fr','en')`,
   demande un `ALTER TYPE` **irréversible**. Non touché : élargir le type TS
   sans la migration ferait planter les saves. À trancher avec l'owner.
3. **Phrase d'enrobage des messages de partage** reste FR/EN (aucun appelant ne
   passe une locale ES aujourd'hui → éviter du code mort). Le *contenu* de la
   fiche partagée, lui, est bien ES via GROQ.

## À tester (côté owner)

- Switcher → ES, parcourir : UI 100 % espagnole (es.json) ; catalogue/fiches
  en FR (fallback) tant que Sanity n'a pas de contenu ES — comportement attendu.
- Une fois le contenu ES dans Sanity : la même fiche bascule en espagnol sans
  redeploy.

## Pas mergé / pas pushé

Commit sur `feat/spanish-locale` uniquement. Merge/push/release en attente de
la décision contenu + d'un « push » explicite de l'owner.
