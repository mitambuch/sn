---
id: session-2026-06-08-1137
date: 2026-06-08
type: session
tags: [#content, #routing, #auth, #client-specific]
scope: client-specific
status: active
---

# Session — Espace membre « en préparation » (un vrai client s'est inscrit)

Branche `feat/member-holding-state`, commit `18a1179`. **Non mergée.**

## Contexte

Owner : un vrai client s'est inscrit sur le live ; il faut « nettoyer les trucs
de merde » car les fiches ne sont pas complètes — contre-productif qu'un client
se connecte et voie du vide/du faux. + question : le gate « Salva accepte » est-il
en place ?

## Réponse au gate d'accès (investigation, pas de code)

Oui, en place. Deux portes : « Demander un accès » = crée seulement un lead
(`access_requests`) + email Salva, **aucun compte** ; Salva accepte dans `/admin`
→ génère un code → email. « J'ai un code » = registerWithCode immédiat (le code
EST l'autorisation). **Personne ne s'auto-inscrit sans code.** Pour la personne
inscrite : vérifier `/admin → demandes d'accès` (lead) vs `/admin → utilisateurs`
(vrai compte). Faille connue : code non lié à l'email du demandeur. Kill switch
`blocked` existe (migration 0025). Cf [[password-access-tunnel]].

## Diagnostic « la merde »

Tout le catalogue membre = **mocks** (`AccountCatalogue`, `CommandPalette` tapent
`@/mocks`) : faux produits, faux comptes à rebours « offre limitée » codés en dur,
contact concierge placeholder (`valmont@sawnext.studio` + faux numéro). Les fixes
crash-safe des fiches sont sur `fix/catalogue-studio-content-model` (non mergée),
mais sans danger live car les listes tapent les mocks pas Sanity.

## Livré — flag `FEATURES.catalogueLive` (false)

Décision complète : [[catalogue-holding-state-flag]]. En bref : nav réduite
(Accueil/Demandes/Profil/Préférences), routes catalogue+modules→redirect /account,
dashboard = greeting + canal demande live + notice « en préparation », Cmd+K
démontée, copy `preparing.*` FR+EN. Réversible (flip → true quand contenu prêt +
merge branche catalogue).

## Validation

`pnpm validate` vert : 522 tests, coverage au-dessus du ratchet, build OK.
`AppLayout.test` recalé sur la nav d'attente ; nouveau `AccountDashboard.test`.
Friction notée (mineure) : `@'...'@` est du PowerShell, pas du Bash → premier
`git commit` via l'outil Bash a foiré commitlint ; refait en heredoc.

## Reste ouvert / à faire owner

1. **OK pour merger + déployer** `feat/member-holding-state` (outward-facing).
2. **Vrai contact concierge** (nom + email + téléphone) pour `siteConfig` → quand
   fourni, on pourra réafficher un vrai bloc concierge.
3. Quand les fiches Sanity sont prêtes : flip `catalogueLive` → true **et** merge
   `fix/catalogue-studio-content-model`. Cf [[catalogue-triple-model-divergence]].
4. `main` local est en avance de 2 commits non poussés (journaux mémoire).

## Release

Ce lot (holding state) est shippable seul. v1.3.0 (tunnel d'accès) toujours en
attente du « go » owner. Recommandation : déployer le holding state vite (un
client est déjà dans l'espace), release à grouper ensuite.
