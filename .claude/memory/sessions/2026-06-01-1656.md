---
id: session-2026-06-01-1656
date: 2026-06-01
type: session
tags: [#auth, #security, #forms, #client-specific]
scope: client-specific
status: active
---

# Session — Bugs client : emails opérateur + formulaire d'accès (après-midi)

## Contexte

Client signale 2 bugs : (1) Salva ne reçoit pas les demandes sur
salva@/info@saw-next.ch, (2) le formulaire d'accès « envoie direct » sans
laisser entrer un message. Puis demande : notifier Salva « quand quelqu'un
s'inscrit » → clarifié = LES DEUX (demande d'accès + création de compte).

## Diagnostic (les 2 bugs sont côté ops/déploiement, pas code)

**Bug formulaire** : le code du wizard 3-steps `AccessRequestModal` est
**correct** — prouvé par un test de régression qui rejoue le parcours
(step Message + bouton Envoyer n'apparaissent qu'en dernier ; steps 0/1
sans bouton submit → Enter ne soumet pas). Cause live = **déploiement
périmé**. Fix = redéployer `main` (Netlify auto-build). Friction
`2026-05-27-access-request-step3-skip` passée en `resolved`.

**Bug emails** : prod tourne encore le sender sandbox Resend
`onboarding@resend.dev` qui ne livre qu'au compte Resend. Migration 0013
(sender brandé) jamais appliquée car saw-next.ch pas vérifié sur Resend
(punch-list session 2026-05-27).

## Ce qui a été fait (branche `fix/access-email-and-form`)

1. `test(access)` — `AccessRequestModal.test.tsx` (2 tests, garde-fou).
2. `fix(supabase)` migration **0014** — `notify_new_inquiry` +
   `notify_new_access_request` → sender `noreply@saw-next.ch`, destinataires
   **salva@ + info@saw-next.ch** (supersede le recipient unique de 0013,
   supprime la dépendance Vault OPERATOR_EMAIL).
3. `feat(supabase)` migration **0015** — `notify_new_signup` : trigger
   AFTER INSERT sur `profiles` → email opérateur à la création de compte.
   POST Resend wrappé en exception guard (un échec email ne bloque jamais
   l'inscription).

## Bloqueurs — ACTION OWNER (accès que je n'ai pas)

1. Redéployer `main` → corrige le formulaire en ligne (merge+push en
   attente du « push » owner).
2. Resend : vérifier le domaine `saw-next.ch` (DNS SPF/DKIM/MX bounce sur
   Infomaniak, additif).
3. Appliquer migrations 0014 + 0015 dans Supabase SQL Editor une fois le
   domaine vérifié. Aucun flip Vault nécessaire (destinataires en dur).

## Next

- Merge + push branche → main (redeploy + migrations dans le repo).
- Smoke test : demande d'accès + inscription réelle → Resend Logs
  « Delivered » sur salva@ et info@.
- RELEASE : feat events + ces correctifs → v1.3.0 une fois en ligne.
